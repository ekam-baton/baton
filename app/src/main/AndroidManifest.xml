<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Camera and Audio (WebRTC) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Media (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Biometric -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Android 17 Localhost Loopback Protection -->
    <uses-permission android:name="android.permission.USE_LOOPBACK_INTERFACE" />

    <application tools:ignore="RemoveWorkManagerInitializer"
        android:name=".BatonApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_baton_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baton_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Baton"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <provider android:name="androidx.work.impl.WorkManagerInitializer" tools:node="remove" tools:ignore="MissingClass" />
        

        <!-- nosemgrep -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Baton"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Secure App Link handler: https://baton-app.in/connect?url=... -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="baton-app.in" android:pathPrefix="/connect" />
            </intent-filter>
        </activity>

        <service
            android:name=".BatonStreamingService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- FCM Push Gateway Service (Pillar 1 — V2) -->
        <service
            android:name=".push.BatonFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
