package com.ekam.baton.feature.agents

import com.journeyapps.barcodescanner.CaptureActivity

/**
 * Custom CaptureActivity to force portrait orientation and allow 
 * enterprise-level layout customizations if needed.
 */
class CustomScannerActivity : CaptureActivity()
